# A股多因子增强策略平台 算法设计说明书

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 文档名称 | A股多因子增强策略平台 算法设计说明书 |
| 文档版本 | V2.1 |
| 关联文档 | 《A股多因子增强策略平台 PRD V2.4》 |
| 文档状态 | 评审版 |
| 适用对象 | 量化研究员、算法工程师、平台研发、测试、风控 |
| 适用市场 | A股 |
| 策略类型 | 中低频、多因子增强、指数增强、组合建议型 |
| 频率 | 周频 / 双周 / 月频为主，日频研究支持 |

---

# 1. 文档目标

## 1.1 编写目的
本文档用于定义 A 股多因子增强策略平台的算法框架、计算口径、数据处理规范、因子体系、评分模型、择时逻辑、风险约束、组合构建方法、回测口径、监控指标与模型迭代机制，作为算法开发、研究评审、测试验收和后续迭代的统一依据。

## 1.2 设计目标
本算法系统目标如下：

1. 构建面向 A 股中低频场景的标准化多因子增强模型  
2. 实现因子研究、模型评分、组合构建、择时控制、回测评估的一体化  
3. 保证模型具备：
   - 可解释性
   - 可回测性
   - 可复现性
   - 可监控性
   - 可商业化交付性
   - 可回退机制（规则基线fallback）
4. 实现因子研究标准流程与上线闸门
5. 实现配置驱动参数管理
6. 为未来扩展机器学习增强模型预留接口

---

# 2. 算法总体架构

## 2.1 分层架构
算法采用六层结构：

### 第一层：数据层
负责原始数据接入、清洗、点时对齐和特征准备，包括：
- 行情数据
- 财务数据
- 股票状态数据
- 指数与行业数据
- 公司行为数据
- 事件数据（业绩预告/快报、问询函、立案处罚、减持公告、股权质押、并购重组、回购、解禁等）
- 数据快照与版本管理（每日snapshot_id，回测绑定快照版本）
- PIT严格版本管理（预告/快报/正式报表优先级管理）

### 第二层：因子层
负责因子定义、计算、预处理、方向统一、中性化与评估，包括：
- 质量成长因子
- 预期修正因子
- 残差动量因子
- 资金流确认因子
- 风险惩罚因子
- 因子元数据系统（factor_name/category/logic/formula/source/pit_required/direction/coverage_threshold/status/version）
- 因子研究标准流程（定义关/数据关/统计关/成本关/去重关/稳健性关/上线关）

### 第三层：评分层
负责多因子合成与综合评分，包括：
- 模块内加权Z-score
- ML融合层
- 模块状态机（active/watch/degraded/disabled）
- 动态权重收缩
- 预测置信度
- 模型注册表

### 第四层：决策层
负责风险预算择时、组合生成和调仓决策，包括：
- 风险预算择时（替代复杂择时）
- 分层赋权组合构建
- 风险折扣
- 双轨制（研究模式Alpha-Risk优化器 / 实盘模式分层赋权）
- 调仓缓冲区设计

### 第五层：风险与执行层
负责风控规则、成本建模与执行约束，包括：
- 黑名单系统（ST/退市整理/重大立案/严重审计非标）
- 风险惩罚规则引擎（商誉高/质押高/减持密集/现金流差/预告恶化/问询频繁）
- 组合风控（行业集中度/风格偏离/小盘暴露/高beta暴露/拥挤度/组合波动/最大预估回撤）
- 极端情景保护（单日波动急升/指数大跌+宽度崩坏/北向大幅流出+高波动→自动降仓）
- 成本模型（佣金/印花税/滑点/冲击成本）
- T+1执行约束

### 第六层：监控与治理层
负责全链路监控、版本管理与实验治理，包括：
- 全链路5类监控（数据监控/因子监控/模型监控/组合监控/实盘监控）
- 因子失效监控（IC/IR/覆盖率/缺失率/PSI/分布漂移）
- 模型漂移监控（预测分布漂移/特征重要性变化/OOF/OOS偏差/SHAP方向漂移）
- 实盘偏差监控（成交偏差/成本偏差/回撤超阈值/连续失效天数）
- 版本管理（因子版本/模型版本/参数版本/快照版本）
- 实验管理（AB实验/灰度发布/回滚机制）
- 告警与回滚（自动响应机制/告警阈值/降权/暂停/降仓/中止）

---

# 3. 数据规范与时点控制

---

## 3.1 数据类别

### 3.1.1 行情数据
- 股票日线 OHLCV
- 成交额、换手率
- 复权因子
- 指数行情

**业务用途**：几乎所有价格类、动量类、波动率类、流动性类、技术指标类因子，都基于这些数据。这是最基础的数据层，后续因子计算的核心依赖。

### 3.1.2 财务数据
- 利润表
- 资产负债表
- 现金流量表
- 财务衍生指标
- 估值指标

**业务用途**：价值因子、质量因子、成长因子、财务异常因子的核心数据来源。关键原则：财务数据必须按公告发布日期（PIT）使用，否则即发生前视偏差——4月30日发布的年报，4月29日之前不可见。

### 3.1.3 股票状态数据
- 上市日期
- 退市日期
- 停牌状态
- ST / *ST 状态
- 涨跌停状态
- 交易状态

**业务用途**：股票池过滤、交易可执行性判断、回测真实化。停牌股无法交易纳入组合也无法执行；ST股涨跌停限制特殊（5%），不适合标准选股逻辑。

### 3.1.4 指数与行业数据
- 指数成分股
- 指数权重
- 行业分类（申万/中信等）
- 行业变更历史

**业务用途**：做超额收益标签（股票收益减基准指数收益）、做择时信号、做组合基准比较、做风险暴露约束。行业分类用于行业中性化、行业约束、行业收益比较和行业内缺失值填充。

### 3.1.5 公司行为数据
- 分红
- 送转
- 配股
- 除权除息
- 代码变更

**业务用途**：回测引擎处理除权除息事件（现金分红计入现金、送股转增调整持仓股数、按持股期限扣税），确保净值计算准确。

### 3.1.6 北向资金数据
- 北向净买入（日频，按股票维度）
- 持股变化（持股数量、持股占比）

**业务用途**：资金流因子模块的核心数据来源，同时用于择时信号。北向资金（外资）是A股最有效的"聪明钱"信号之一。

### 3.1.7 分析师数据
- 评级修正（上调/下调/维持）
- 覆盖度（覆盖分析师数量）
- 盈利预期变化（一致预期EPS修正）
- 盈利惊喜（实际值vs一致预期偏差）

**业务用途**：预期修正模块的核心数据来源。分析师预期代表市场共识，预期修正往往领先于价格反应（PEAD效应），SUE信号捕捉盈利惊喜的超额收益漂移。

### 3.1.8 融资融券/大单/微观结构数据
- 融资余额（日频）
- 大单占比（大单买入额/总成交额）
- 隔夜收益（今日开盘价/昨日收盘价 - 1）
- VPIN（成交量概率知情交易）
- Amihud非流动性

**业务用途**：微观结构Alpha、事件驱动信号、资金流跟踪、流动性与冲击成本估计。隔夜收益反映信息冲击，VPIN衡量订单流不平衡，融资余额变化反映杠杆资金动向。

### 3.1.9 事件数据
- 业绩预告、业绩快报
- 年报季报披露
- 问询函、立案处罚
- 减持公告
- 股权质押
- 并购重组
- 回购
- 解禁

**业务用途**：风险惩罚模块的核心数据来源。问询函/立案处罚/减持密集/股权质押等事件，是个股风险排雷的关键信号，用于风险折扣和黑名单过滤。

### 3.1.10 数据快照与版本管理
- 每日snapshot_id，标识当日数据版本
- 回测绑定快照版本，确保可复现
- 因子值快照与标签快照独立版本管理

---

## 3.2 点时一致性要求

### 3.2.1 核心原则
所有因子、标签、股票池和回测都必须遵守“**交易时点可见性原则**”：

> 在交易日 t 生成信号时，只允许使用 t 日收盘前或系统定义可见时点之前可获得的数据。

### 3.2.2 财务数据处理
财务数据必须按**公告发布日期**而不是财报所属期间使用。

即：
- Q1 财报不能在 3 月 31 日使用
- 只能在实际披露日后进入因子计算

### 3.2.3 指数成分处理
指数成分及权重必须按历史时间回溯，不可使用当前成分回填历史。

### 3.2.4 股票状态处理
停牌、ST、退市整理、涨跌停状态必须按交易日快照使用。

### 3.2.5 PIT财务数据严格版本管理
财务数据需维护严格的版本管理，确保回测与实盘一致性：

- 优先级规则：正式公告 > 业绩快报 > 业绩预告
- 每条财务记录保留字段：report_period / announce_date / effective_date / source_priority / revision_no
- 同一report_period数据，仅使用effective_date≤当前交易日的最高优先级版本
- 业绩预告被业绩快报替代后，不再使用预告数据计算因子
- 业绩快报被正式公告替代后，不再使用快报数据

### 3.2.6 分析师与北向资金PIT对齐
- 分析师评级修正：按实际发布日期对齐，T日发布的修正T+1日可用
- 北向资金数据：T日数据T日收盘后可用（盘中数据不参与信号生成）
- 盈利预期变化：按一致预期更新日期对齐

---

## 3.3 数据质量检查

数据进来后不会直接使用，而是先做质量检查。设计原则：**宁可保守，也不要让错误数据污染信号。**

### 3.3.1 基础校验
- OHLC逻辑一致性：`high >= max(open, close)`，`low <= min(open, close)`
- 成交额、成交量非负
- 涨跌幅与前收盘逻辑一致

### 3.3.2 覆盖率检查
- 某字段当天是否大面积缺失
- 是否有行业、板块、个股维度的异常断档

### 3.3.3 多源交叉验证
- 同一股票同一天的数据，在不同源之间比对
- 若差异超过阈值，标记异常

### 3.3.4 异常标记
- 不一定立即删除
- 先打标，避免脏数据进入因子和训练流程

---

## 3.4 数据预处理规范
所有因子进入评分前，需统一执行以下步骤（**严格顺序，不可调换**）：

1. **对齐股票池** — 将因子值按当日股票池对齐，非池内股票剔除
2. **缺失标记** — 生成"是否缺失"指示变量（后续ML扩展可用）
3. **覆盖率检查** — 低于70%停用该因子当日
4. **行业内填充 / PIT衰减前向填充** — 财务数据用exp(-decay_rate×距公告日天数)衰减
5. **去极值** — MAD: median ± 3 × 1.4826 × MAD，截断不删除（MAD=0时回退到IQR分位数截断）
6. **行业+市值中性化** — 回归取残差，必须在标准化之前
7. **标准化** — Z-score / 逆正态秩变换 / 稳健Z-score
8. **方向统一** — 所有因子越大越好
9. **正交化**（可选）— 截面回归取残差消除共线性
10. **存因子快照** — 保存当日因子值快照，绑定snapshot_id

> **V2.1 修正**：中性化必须在标准化之前执行，否则残差不再服从标准正态分布。原因：中性化是回归取残差，残差已经不再服从原始分布；如果先标准化再中性化，残差不再服从标准正态，标准化就失效了。完整预处理流程从8步扩展为10步，新增缺失标记、PIT衰减填充、正交化、快照存储。

---

# 4. 标签与评价目标设计

本平台主策略为多因子增强，核心标签体系用于评估与ML融合训练。

---

## 4.1 评价目标

**为什么不预测绝对收益**：绝对收益很容易被指数行情主导。牛市里什么都涨，模型看起来很准，其实只是押中了市场Beta。所以更合理的是预测超额收益——模型要找到真正能跑赢指数的股票。

平台主要评价以下目标：

- 未来 N 日绝对收益
- 未来 N 日相对基准超额收益
- 未来 N 日行业中性超额收益
- 分组收益表现
- 多空组合收益表现

---

## 4.2 标签定义

### 4.2.1 主标签
\[
y_{i,t}^{main} = future\_20d\_excess\_return
\]

基准指数：中证800

### 4.2.2 辅标签
- 5日超额收益
- 10日超额收益
- 60日超额收益

### 4.2.3 分类标签
是否进入前20%：
\[
y_{i,t}^{class} = \begin{cases} 1, & rank\_pct \le 0.20 \\ 0, & otherwise \end{cases}
\]

### 4.2.4 未来 N 日超额收益
\[
y_{i,t}^{excess}(N)=r_{i,t+1:t+N}-r_{b,t+1:t+N}
\]

其中：
- \(r_i\) 为个股未来收益
- \(r_b\) 为基准指数未来收益

### 4.2.5 行业中性超额收益
\[
y_{i,t}^{ind\_excess}(N)=r_{i,t+1:t+N}-median(r_{industry,t+1:t+N})
\]

### 4.2.6 标签截尾与标准化
- **标签截尾**：按日横截面winsorize到1%/99%，防止极端值主导
- **标签标准化**：线性模型使用标准化标签（Z-score），树模型使用原始截尾标签

---

## 4.3 评价周期
建议支持以下周期：
- 5日
- 10日
- 20日

平台首期主评估周期推荐：
- 周频策略：5日 / 10日
- 月频策略：20日

---

# 5. 股票池构建算法

---

## 5.1 股票池目标
构建满足可交易性、流动性和风险控制要求的候选股票集合。

**业务原理**：股票池构建是第一道风控。很多人误以为多因子系统是对"所有股票"打分，其实不是。在A股，股票池构建本身就是收益与风险控制的重要部分——先决定"谁有资格被选"，再决定"谁更优"。

## 5.2 基础股票池
支持以下基础池：
- 沪深300
- 中证500
- 中证1000
- 中证全指
- 自定义池

系统支持两类股票池：
- **核心池**：中大市值 + 高流动性，更稳健，更适合机构/大资金，偏稳健增强
- **扩展池**：覆盖更多股票，Alpha来源更多，但噪音更大、交易成本更高，偏进取Alpha

## 5.3 过滤规则
在每个调仓日 t，对基础池执行过滤：

### 5.3.1 状态过滤
剔除：
- ST / *ST — 财务/退市风险高，涨跌停限制特殊（5%），不适合标准交易逻辑
- 停牌股票 — 无法交易，无法调仓，容易卡住资金
- 退市整理股票 — 极高风险，不具备正常选股意义

### 5.3.2 上市时长过滤
剔除上市未满 \(N_{list}\) 天股票
建议默认：
\[
N_{list} = 180
\]

**业务原因**：新股历史数据不足，因子不稳定；新股波动极大，容易出现一字板；上市初期价格行为异常，不适合纳入常规选股。

### 5.3.3 流动性过滤
剔除近 \(N_{liq}\) 日日均成交额低于阈值股票：

\[
AvgAmount_{i,t}(N_{liq}) < Threshold_{amount}
\]

建议默认：
- \(N_{liq}=20\)
- \(Threshold_{amount} = 8000万\)

**业务原因**：流动性差的股票买得进去但卖不出来，或者交易本身就会大幅影响价格（冲击成本过高），实盘容量不足。

### 5.3.4 总市值过滤
剔除总市值低于阈值股票：
\[
MV_{i,t} < 80亿
\]

**业务原因**：小市值股票更容易被操纵，波动率异常，容量差，噪音高。

### 5.3.5 价格过滤
剔除极端低价股，例如：
\[
Price_{i,t} < 3
\]

**业务原因**：低价股通常是问题股，绝对价格过低导致交易颗粒度失真（A股最小交易单位100股），容易聚集壳价值和投机噪音。

### 5.3.6 风险事件过滤
剔除近60日存在重大立案/处罚/严重审计问题的股票。

### 5.3.7 黑名单硬过滤
剔除黑名单中股票（ST/退市整理/重大立案/严重审计非标等）。

### 5.3.8 交易可行性过滤
剔除调仓执行日可能无法成交股票：
- 一字涨停
- 一字跌停
- 长期停牌刚复牌且流动性异常

---

## 5.4 股票池输出
输出内容包括：
- 股票池列表
- 过滤前后样本数
- 每只股票被剔除原因
- 股票池版本号

---

# 6. 因子体系设计

---

## 6.1 因子体系设计原则

因子体系采用模块化架构，分为四大Alpha模块，每个模块聚焦不同维度的信息，预测不同的目标周期：

| 模块 | 信息维度 | 预测目标周期 | Alpha来源 |
|------|---------|-------------|----------|
| 价格模块 | 量价行为 | 5-10日 | 短期动量、反转、波动率异常 |
| 价值模块 | 基本面估值 | 10-20日 | 价值回归、盈利质量 |
| 修正模块 | 预期调整 | 5-20日 | PEAD效应、SUE信号 |
| 资金流模块 | 资金动向 | 3-10日 | 聪明钱跟踪、资金驱动 |

**设计原理**：四大模块覆盖不同信息维度和不同预测周期，互为补充。价格模块捕捉短期技术信号，价值模块捕捉中期基本面回归，修正模块捕捉预期调整漂移，资金流模块捕捉短期资金驱动。模块间低相关，融合后信号更稳健。

因子设计遵循以下原则：

1. 有基本经济或行为金融逻辑  
2. 能在 A 股历史中验证有效性  
3. 覆盖率足够高  
4. 稳定性较好  
5. 与已有因子冗余度可控  
6. 可解释、可维护、可扩展  

---

## 6.2 因子分类与定义

V2.0采用5模块+1风险模块架构，替代原6类因子分类：

---

### 6.2.1 质量成长模块
反映企业盈利能力、经营质量和成长性，模块内等权。

- ROE_TTM
\[
ROE_{TTM} = \frac{NetProfit_{TTM}}{Equity}
\]
- ROA_TTM
\[
ROA_{TTM} = \frac{NetProfit_{TTM}}{TotalAssets}
\]
- 毛利率
\[
GrossMargin = \frac{Revenue - Cost}{Revenue}
\]
- CFO / 净利润比
\[
CFO\_NP = \frac{OperatingCashFlow}{NetProfit}
\]
- 资产负债率（反向）
\[
LEV = -\frac{TotalLiabilities}{TotalAssets}
\]
- 营收增速
\[
Growth_{rev} = \frac{Revenue_t - Revenue_{t-4}}{Revenue_{t-4}}
\]
- 净利润增速
\[
Growth_{np} = \frac{NetProfit_t - NetProfit_{t-4}}{NetProfit_{t-4}}
\]
- Sloan应计（反向）
\[
Sloan = -\frac{NetProfit - OperatingCashFlow}{TotalAssets}
\]

---

### 6.2.2 预期修正模块
反映市场预期差与修正信号。

| 因子 | 权重 |
|------|------|
| SUE（标准化未预期盈利） | 0.25 |
| 盈利惊喜（实际值vs一致预期偏差） | 0.25 |
| 分析师1月修正 | 0.25 |
| 覆盖度变化 | 0.10 |
| PEAD（盈利公告后漂移） | 0.15 |

---

### 6.2.3 残差动量模块
反映价格趋势延续与反转特征，去除行业/市场影响后的动量。

| 因子 | 权重 |
|------|------|
| 12月动量跳1月 | 0.30 |
| 6月动量跳1月 | 0.20 |
| 残差动量 | 0.25 |
| 1月反转 | 0.10 |
| 低波动率 | 0.15 |

---

### 6.2.4 资金流确认模块
反映机构与聪明资金的确认信号。

| 因子 | 权重 |
|------|------|
| 北向净买入 | 0.50 |
| 换手率趋势 | 0.30 |
| Amihud非流动性 | 0.20 |

---

### 6.2.5 风险惩罚模块
反映个股风险特征，用于扣分而非加分。

| 因子 | 权重 |
|------|------|
| 商誉占比 | 0.15 |
| 股权质押 | 0.10 |
| 业绩预告下修 | 0.15 |
| 审计异常 | 0.20 |
| 问询立案 | 0.20 |
| 现金流异常 | 0.10 |
| 减持 | 0.10 |

---

## 6.3 因子元数据系统

每个因子需维护完整元数据记录：

| 字段 | 说明 |
|------|------|
| factor_name | 因子名称 |
| category | 所属模块 |
| logic | 经济逻辑描述 |
| formula | 计算公式 |
| source | 数据来源 |
| pit_required | 是否需要PIT对齐 |
| direction | 方向（越大越好/越小越好） |
| coverage_threshold | 最低覆盖率要求 |
| status | 状态（research/active/watch/deprecated） |
| version | 版本号 |

---

## 6.4 因子研究标准流程（7关）

新因子上线需通过以下7关：

1. **定义关** — 因子有清晰经济逻辑，非数据挖掘
2. **数据关** — 数据源可靠，PIT对齐，覆盖率>70%
3. **统计关** — RankIC均值>0，正IC比例>52%，ICIR>0.5
4. **成本关** — 考虑换手成本后仍有边际价值
5. **去重关** — 与现有因子相关性<0.7，否则需证明边际贡献
6. **稳健性关** — 分年度不极端，不依赖单一市场环境
7. **上线关** — 样本外验证通过，文档完整，监控配置就绪

---

## 6.5 因子上线闸门

因子上线需满足以下全部条件：
- 覆盖率 > 70%
- RankIC均值 > 0
- 正IC比例 > 52%
- 分年度不极端（无单一年份贡献超50%）
- 成本后有边际价值（扣除双倍换手成本后IC仍显著）

---

## 6.6 因子预处理

### 6.6.1 缺失值处理
支持：
- 删除覆盖率过低因子
- 行业内均值填充
- 横截面中位数填充
- 保留缺失标记（后续 ML 扩展）

### 6.6.2 去极值
支持：
- MAD 去极值
- 分位数截断（1% / 99%）

MAD 方法：
\[
x' = median(x) \pm k \cdot MAD
\]

### 6.6.3 标准化
采用横截面 Z-score：
\[
z_i = \frac{x_i - \mu_x}{\sigma_x}
\]

### 6.6.4 因子方向统一
统一为：
- 分数越高，预期未来表现越好

例如：
- PB 越低越好，则取负后标准化或标准化后取反

### 6.6.5 中性化
#### 行业中性化
对因子做行业哑变量回归，取残差作为中性化因子值。

\[
x_i = \alpha + \sum_k \beta_k Industry_{i,k} + \epsilon_i
\]

#### 市值中性化
加入 log(market cap) 控制项：

\[
x_i = \alpha + \beta \log(MV_i) + \sum_k \gamma_k Industry_{i,k} + \epsilon_i
\]

残差 \(\epsilon_i\) 作为中性化后因子。

---

# 7. 单因子评估方法

---

## 7.1 IC
定义为因子值与未来收益之间的横截面相关性：

\[
IC_t = Corr(x_t, y_t)
\]

## 7.2 RankIC
定义为因子排序与未来收益排序之间的相关性：

\[
RankIC_t = Corr(rank(x_t), rank(y_t))
\]

## 7.3 ICIR
\[
ICIR = \frac{mean(IC)}{std(IC)}
\]

## 7.4 分层回测
将因子值按横截面分为 Q1~Q5 或 D1~D10，观察未来收益。

关键指标：
- Top组收益
- Bottom组收益
- Top-Bottom 多空收益
- 单调性

## 7.5 衰减分析
对 1/5/10/20 日未来收益分别计算 IC，观察信号衰减速度。

## 7.6 稳定性分析
按年份、市场风格阶段、牛熊震荡阶段进行子样本分析。

## 7.7 年度稳定性
分年份IC不能严重单一年份贡献（单年贡献不超过50%）。

## 7.8 行业/市值分组表现
按行业和市值分组检验因子表现是否均匀，避免因子仅在特定行业/市值区间有效。

## 7.9 换手率与成本后收益
评估因子换手率，扣除双倍换手成本后检验因子是否仍有边际价值。

## 7.10 与现有因子相关性与边际贡献
新因子与现有因子池的相关性分析，相关系数>0.7时需证明边际贡献（增量IC回归F检验显著）。

---

# 8. 多因子评分模型设计

---

## 8.1 目标
将多个单因子合成为综合评分，用于股票排序与候选池筛选。V2.0采用模块化打分+ML融合架构。

## 8.2 模块打分流程

### 8.2.1 模块内打分
每个模块内：
1. 每个因子横截面Z-score
2. 按预设权重加权
3. 加权结果再做横截面Z-score

\[
S_{module,i} = Z\left(\sum_{k \in module} w_k \cdot Z(x_{i,k})\right)
\]

### 8.2.2 V2基线模块权重

| 模块 | 基线权重 |
|------|---------|
| 质量成长 | 35% |
| 预期修正 | 30% |
| 残差动量 | 25% |
| 资金流确认 | 10% |

### 8.2.3 风险惩罚
综合评分扣除风险惩罚分：
\[
S_{final} = S_{raw} - 0.35 \times P_{risk}
\]

其中 \(P_{risk}\) 为风险惩罚模块得分（0~1范围）。

## 8.3 信号融合流程

四个模块分数不能简单等权平均，因为不同市场环境下各模块的有效性不同。融合公式：
\[
S_{final} = \sum_m w_m \cdot z(S_m)
\]
先横截面标准化再加权。

**融合5步流程**：

1. **基础权重**：质量成长35%、预期修正30%、残差动量25%、资金流确认10%。这是长期均衡权重（先验配置）。
2. **动态IC加权**：用各模块近60个交易日的IC历史，计算IR = IC均值/IC标准差。IR高且稳定的模块权重大，IC波动大的模块被惩罚。混合方式：最终权重 = 70% × 基础权重 + 30% × 动态权重，避免过度切换。
3. **Regime调权**：根据当前市场状态调整权重（详见8.4节）。
4. **高相关模块收缩**：如果两个模块近期分数高度相关（相关系数>0.7），说明信息重复，对权重较小的模块做收缩（乘以0.5），避免重复计算同一信息。
5. **归一化融合**：归一化后加权融合，最终分数再做一次横截面Z-score标准化。

## 8.4 动态权重收缩

为防止权重漂移过度，采用基线+动态收缩：
\[
w = 0.7 \times w_{base} + 0.3 \times w_{dynamic\_shrunk}
\]

权重限制：每个模块权重在10%~45%之间。

## 8.5 Regime调权

根据市场状态调整模块权重：

| 市场状态 | 质量成长 | 预期修正 | 残差动量 | 资金流确认 |
|---------|---------|---------|---------|-----------|
| 趋势市 | 30% | 25% | 30% | 15% |
| 震荡市 | 40% | 30% | 20% | 10% |
| 防御市 | 45% | 25% | 15% | 15% |
| 进攻市 | 25% | 30% | 35% | 10% |

## 8.6 高相关模块收缩

当两个模块相关系数>0.7时，对权重较小模块乘以0.5收缩系数，防止双重暴露。

## 8.7 模块状态机

| 状态 | 说明 |
|------|------|
| active | 正常参与评分 |
| watch | IC近期下降，权重收缩50% |
| degraded | IC持续恶化，权重收缩至10% |
| disabled | 因子失效，完全排除 |

状态转换规则：
- active → watch：连续4周RankIC均值<0.02
- watch → degraded：连续8周RankIC均值<0
- degraded → disabled：连续12周RankIC均值<0
- watch → active：连续4周RankIC恢复到>0.03

## 8.8 ML增强排序

V2.0采用两阶段结构：模块内聚合 → ML融合层。

详见第15章"机器学习增强算法设计"。

### 8.7.1 三路融合
\[
score_{final} = 0.30 \times score_{rule} + 0.25 \times score_{ridge} + 0.45 \times score_{lgbm}
\]

### 8.7.2 预测置信度
基于以下维度计算预测置信度（0~1）：
- 模块一致性（各模块排名方向一致性）
- 特征缺失率（缺失特征占比越高，置信度越低）
- 模型稳定性（近期预测排名波动）
- 流动性（流动性越差，置信度越低）
- 风险标签（有风险标签的股票置信度降低）

## 8.8 模型注册表

每个模型版本需注册以下信息：
- 模型ID / 版本号
- 训练数据范围
- 特征列表
- 超参数
- OOS指标（IC/IR/多空收益）
- 上线日期 / 灰度比例
- 负责人 / 审批状态

---

# 9. 风险预算择时算法设计

---

## 9.1 目标
控制组合风险暴露，通过风险预算调节仓位水平，而非预测市场涨跌。目标从"预测市场方向"转为"风险暴露调节"。

## 9.2 择时信号

### 9.2.1 指数中期趋势
- MA20 > MA60：记+1
- MA60 > MA120：记+1
- 否则记0
- 信号得分范围：0~2

### 9.2.2 市场宽度
定义上涨股票占比或站上均线股票占比：

\[
Breadth_t = \frac{\#UpStocks_t}{\#AllStocks_t}
\]

- > 60%：强（正面）
- 45%~60%：中（中性）
- < 45%：弱（负面）

### 9.2.3 市场波动率
- 低波动（VIX等效<历史25%分位）：正面
- 中波动（25%~75%分位）：中性
- 高波动（>75%分位）：负面

### 9.2.4 北向资金趋势
- 近20日净流入均值显著为正：+1
- 近20日净流入均值显著为负：-1
- 否则：0

### 9.2.5 组合自身回撤
作为回撤保护触发信号，非择时信号本身。

---

## 9.3 仓位输出

综合5个信号后输出4档仓位：

| 信号综合得分 | 目标仓位 |
|-------------|---------|
| 强正面 | 100% |
| 正面 | 80% |
| 中性 | 60% |
| 负面 | 30% |

---

## 9.4 回撤保护规则

无论择时信号如何，回撤保护优先：

- 组合回撤 > 5%：仓位降一档
- 组合回撤 > 8%：仓位再降一档
- 组合回撤 > 12%：最低仓位30%

回撤恢复后（从高点回撤恢复到3%以内），逐步恢复仓位。

---

## 9.5 弃用与降级

以下方法移至实验层，不在主策略中使用：
- HMM隐马尔可夫择时
- DMA动态多因子择时
- 季节性择时
- 复杂贝叶斯择时

这些方法可作为研究参考，但不得进入生产信号。

---

# 10. 组合构建算法设计

---

## 10.1 目标
将综合评分转化为可执行持仓组合。

## 10.2 候选股生成
对股票池按 Score 排序，选取 Top N 作为候选池。

推荐：
- HS300增强：Top 30~80
- 中证500增强：Top 50~100
- 中证1000增强：Top 80~150

V2.0默认：Top 60

---

## 10.3 分层赋权方法（V2.0新增）

Top 60持仓采用分层赋权：

| 排名区间 | 赋权系数 | 说明 |
|---------|---------|------|
| 1~10名 | 1.5x | 高置信度超配 |
| 11~30名 | 1.2x | 中置信度适度超配 |
| 31~60名 | 1.0x | 基准权重 |

最终权重归一化到100%。

---

## 10.4 风险折扣层（V2.0新增）

每只股票的权重需经过风险折扣：

\[
w_i^{final} = w_i^{base} \times D_{risk,i} \times D_{liq,i}
\]

### 10.4.1 风险折扣 D_risk

| 风险等级 | D_risk | 判定标准 |
|---------|--------|---------|
| 低风险 | 1.00 | 无风险标签 |
| 中风险 | 0.85 | 1个风险标签 |
| 高风险 | 0.60 | 2~3个风险标签 |
| 极高风险 | 0.00 | >3个风险标签或黑名单 |

### 10.4.2 流动性折扣 D_liq

| 流动性等级 | D_liq | 判定标准 |
|-----------|-------|---------|
| 正常 | 1.00 | 日均成交额>1亿 |
| 边缘 | 0.85 | 日均成交额8000万~1亿 |
| 明显不足 | 0.60 | 日均成交额<8000万 |

---

## 10.5 双轨制（V2.0新增）

| 模式 | 适用场景 | 方法 |
|------|---------|------|
| 研究模式 | 回测研究 | Alpha-Risk优化器（二次规划） |
| 实盘模式 | 实盘交易 | 分层赋权 + 风险折扣 |

---

## 10.6 Alpha-Risk优化器（研究模式）

### 10.6.1 目标函数
\[
\max \alpha'w - \frac{\lambda}{2} w'\Sigma w - \gamma ||w - w_{prev}||_1 - \eta \times ExposurePenalty
\]

其中：
- \(\alpha\)：综合评分向量
- \(\Sigma\)：协方差矩阵
- \(w_{prev}\)：上期持仓权重
- \(ExposurePenalty\)：行业/风格暴露惩罚

### 10.6.2 约束条件
- 单票权重 ≤ 3%
- 行业偏离 ≤ 5%
- 仅做多（\(w_i \ge 0\)）
- 权重之和 = 100%

---

## 10.7 权重分配方式（兼容V1）

### 10.7.1 等权
\[
w_i = \frac{1}{N}
\]

### 10.7.2 分数加权
\[
w_i = \frac{\max(Score_i,0)}{\sum_j \max(Score_j,0)}
\]

### 10.7.3 基准增强权重
\[
w_i = w_i^{benchmark} + \Delta w_i
\]

其中 \(\Delta w_i\) 由综合评分和约束决定。

---

## 10.8 约束条件

### 10.8.1 单股约束
\[
w_i \le 3\%
\]

### 10.8.2 行业偏离约束
\[
|W_{industry} - W_{industry}^{benchmark}| \le 5\%
\]

### 10.8.3 持仓数约束
\[
N_{min} \le N_{holdings} \le N_{max}
\]

### 10.8.4 换手约束
\[
Turnover_t \le T_{max}
\]

### 10.8.5 流动性约束
个股目标买卖金额不能超过近 N 日平均成交额的某一比例。

---

## 10.9 调仓缓冲区与换手控制（V2.0更新）

### 10.9.1 缓冲区设计
- 当前持仓若排名仍在 Top 75 内，则可继续持有
- 新进股票需进入 Top 50 才买入
- 最小调仓阈值：0.20%（低于此不调仓）

### 10.9.2 换手控制
- 单次调仓换手率上限由策略类型设定
- 换手率 = \(\frac{1}{2}\sum|w_{i,t} - w_{i,t-1}|\)

---

## 10.10 100股整数倍处理

实盘模式下，所有持仓股数必须为100的整数倍：
- 买入：向下取整到100的整数倍
- 剩余资金分配给权重最高且可增加持仓的股票
- 卖出：按目标权重计算股数后向下取整

---

# 11. 风险模型与风控算法设计

---

## 11.1 协方差矩阵估计

支持以下方法：

| 方法 | 说明 | 适用场景 |
|------|------|---------|
| 样本协方差 | 直接计算 | 样本充足时基线 |
| Ledoit-Wolf压缩 | 向单位矩阵压缩 | 高维小样本 |
| EWMA | 指数加权移动平均 | 近期波动更重要 |
| DCC-GARCH | 动态条件相关 | 时变相关性建模 |
| PCA混合 | 因子模型+特质风险 | 大规模资产池 |

---

## 11.2 Barra风险模型

### 11.2.1 风格因子（10个）
市值/波动率/流动性/价值/成长/盈利/杠杆/动量/换手率/质量

### 11.2.2 因子暴露度
每只股票对10个风格因子的暴露度，按月度更新。

### 11.2.3 因子收益
WLS截面回归估计因子收益：
\[
r_i = \sum_k \beta_{i,k} f_k + \epsilon_i
\]

### 11.2.4 因子协方差
EWMA + 特征值裁剪，防止估计噪声放大。

### 11.2.5 特质方差
分行业EWMA估计个股特质方差。

---

## 11.3 VaR/CVaR

### 11.3.1 Student-t分布VaR
\[
VaR_\alpha = \mu + t_{\alpha,\nu} \times \sigma \times \sqrt{\frac{\nu-2}{\nu}}
\]

### 11.3.2 流动性调整VaR
在标准VaR基础上增加流动性调整项：
\[
VaR^{liq} = VaR + \frac{1}{2} \times Spread \times Position
\]

### 11.3.3 Copula尾部依赖
使用Copula模型捕捉资产间尾部依赖关系，用于极端情景VaR估计。

### 11.3.4 VaR回测检验
- **Kupiec POF检验**：检验VaR突破频率是否显著偏离预期
- **Christoffersen独立性检验**：检验VaR突破是否独立分布

---

## 11.4 黑名单系统

以下类型股票自动加入黑名单，硬过滤：

| 类型 | 判定标准 |
|------|---------|
| ST/*ST | 交易所ST标识 |
| 退市整理 | 进入退市整理期 |
| 重大立案 | 近60日证监会立案调查 |
| 严重审计非标 | 审计意见为保留/否定/无法表示 |

---

## 11.5 风险惩罚规则引擎

基于规则对个股施加风险惩罚（0~1分）：

| 规则 | 触发条件 | 惩罚权重 |
|------|---------|---------|
| 商誉高 | 商誉/净资产 > 50% | 0.15 |
| 质押高 | 质押比例 > 50% | 0.10 |
| 减持密集 | 近30日重要股东减持>1% | 0.10 |
| 现金流差 | CFO/净利润 < 0.5 | 0.10 |
| 预告恶化 | 业绩预告下修 | 0.15 |
| 问询频繁 | 近60日收到问询函 | 0.20 |

---

## 11.6 组合风控

| 风控维度 | 阈值 | 处置 |
|---------|------|------|
| 行业集中度 | 单行业>30% | 超配部分降权 |
| 风格偏离 | 单因子暴露>1.5σ | 偏离部分收缩 |
| 小盘暴露 | 小盘因子暴露>1.0σ | 收缩小盘持仓 |
| 高beta暴露 | 组合beta>1.3 | 降beta暴露 |
| 拥挤度 | 持仓与主流基金重仓重叠>40% | 降低拥挤持仓 |
| 组合波动 | 年化波动>25% | 整体降仓 |
| 最大预估回撤 | 预估回撤>15% | 触发风控降仓 |

---

## 11.7 极端情景保护

以下极端情景触发自动降仓：

| 情景 | 触发条件 | 响应 |
|------|---------|------|
| 单日波动急升 | VIX等效>历史95%分位 | 仓位降至60% |
| 指数大跌+宽度崩坏 | 指数跌>3%且宽度<30% | 仓位降至30% |
| 北向大幅流出+高波动 | 北向净流出>50亿且波动率>75%分位 | 仓位降至30% |

---

## 11.8 三层风控体系

| 层级 | 名称 | 覆盖范围 |
|------|------|---------|
| 第一层 | 准入风控 | 黑名单/风险事件过滤/上市天数/流动性 |
| 第二层 | 持仓风控 | 风险折扣/风险惩罚/单票上限/行业偏离 |
| 第三层 | 组合风控 | 组合波动/回撤保护/极端情景/拥挤度 |

---

# 12. 交易成本与可交易性建模

---

## 12.1 成本构成
A股交易成本包括：
- 佣金
- 印花税（卖出）
- 过户费
- 滑点
- 冲击成本（简化建模）

## 12.2 成本模型
总成本：
\[
Cost = Commission + StampTax + TransferFee + Slippage
\]

### 12.2.1 滑点模型
简化为成交金额比例：
\[
Slippage = \eta \cdot Amount
\]

或按流动性分层设定不同滑点参数。

## 12.2.2 可交易性约束
若个股当日：
- 停牌
- 一字涨停无法买入
- 一字跌停无法卖出

则该部分订单视为未成交，延后处理。

---

# 13. 回测引擎算法规则

---

## 13.1 回测频率
支持：
- 日频信号研究
- 周频调仓
- 双周调仓
- 月频调仓

## 13.2 执行时点
默认规则：
- t 日收盘后生成信号
- t+1 日执行交易

## 13.3 成交假设
支持：
- 次日开盘价成交
- 次日 VWAP 成交
- 加滑点成交

## 13.4 A股交易规则
必须支持：
- T+1（当日买入部分不可卖出，原有持仓仍可卖出，精确到股数追踪）
- 涨跌停（主板±10%、创业板/科创板±20%、ST±5%、北交所±30%）
- 停牌
- 100股整数倍
- 除权除息（现金分红+送股转增，按持股期限扣税：>1年0%、>30天10%、≤30天20%）
- 股票池动态变化

## 13.4.1 分红派息处理
回测引擎需处理除权除息事件：
- **现金分红**：每股现金分红 × 持仓股数 × (1 - 税率) 计入现金账户
- **送股/转增**：调整持仓股数和成本价，送股部分按面值1元扣税
- **持股期限税率**：>1年免税、>30天10%、≤30天20%
- **面板数据安全**：动量/波动率因子计算使用 `groupby('ts_code')` 确保rolling/shift不跨股票边界

## 13.5 回测输出
- 净值曲线
- 超额收益曲线
- 回撤曲线
- 月度收益
- 行业暴露
- 风格暴露
- 换手率
- 成本侵蚀率
- 调仓记录

## 13.6 稳健性测试框架

参数扰动测试，检验策略对参数敏感度：
- 股票池阈值扰动（上市天数±20%、成交额±20%）
- 调仓频率变化（周频/双周/月频）
- 权重参数扰动（模块权重±5%）
- 单票上限变化（2%/3%/5%）
- 行业约束变化（3%/5%/8%）

## 13.7 压力测试框架

模拟不利交易条件：
- 更高滑点（2x/3x正常滑点）
- 更高手续费（2x/3x正常佣金）
- 延迟执行VWAP（T+2执行）
- 随机滑点扰动（Monte Carlo模拟）

## 13.8 分层归因系统

- **模块归因**：各Alpha模块贡献分解
- **行业归因**：行业配置收益vs选股收益
- **风格归因**：风格暴露收益贡献
- **择时归因**：仓位调整带来的收益/损失
- **个股归因**：Top/Bottom个股收益贡献

## 13.9 分层回测

- 分年份回测
- 分市况回测（牛市/熊市/震荡市）
- 分行业回测

## 13.10 容量测试

评估策略资金容量：
- 不同资金规模（1亿/5亿/10亿/50亿）
- 不同参与率（5%/10%/20%）
- 冲击成本建模（按成交额比例递增）
- 收益衰减曲线

## 13.11 实盘偏差归因

- 模型信号 vs 实际持仓偏差
- 理想成交 vs 实际成交偏差
- 成本偏差（预估成本vs实际成本）
- 未成交损失（无法执行订单的信号损失）

## 13.12 Walk-Forward验证

采用滚动窗口Walk-Forward验证：
- 训练窗口：36个月
- 验证窗口：12个月
- 步进：1个月
- 仅统计OOS（Out-of-Sample）结果

## 13.13 统计检验

- **蒙特卡洛置换检验**：随机置换标签1000次，计算p值
- **DSR通胀夏普比率**：修正数据窥探偏差后的夏普比率
- **最小回测长度MinBTL**：确保回测期数满足统计显著性
- **Bootstrap置信区间**：对关键指标计算95%置信区间

---

# 14. 全链路监控与模型失效识别

---

## 14.1 数据监控
监控：
- 覆盖率（各数据源字段覆盖率）
- 空值率（关键字段空值率异常升高）
- 异常值比例（超出合理范围的数据占比）
- 多源冲突率（同一数据多源不一致比例）
- PIT失败率（PIT对齐失败比例）

## 14.2 因子监控
监控：
- Rolling IC / Rolling RankIC
- ICIR
- 覆盖率 / 缺失率
- PSI（群体稳定性指数）
- 分布漂移

## 14.3 模型监控
监控：
- 预测分布漂移（预测得分分布变化）
- 特征重要性变化
- OOF/OOS偏差
- SHAP方向漂移

## 14.4 组合监控
监控：
- 行业暴露偏离
- 风格暴露偏离
- 换手率
- 风险预算使用率
- 持仓拥挤度

## 14.5 实盘监控
监控：
- 成交偏差（目标vs实际持仓偏差）
- 成本偏差（预估vs实际成本偏差）
- 回撤超阈值
- 连续失效天数

## 14.6 自动响应机制

| 异常类型 | 响应动作 |
|---------|---------|
| 因子失效（IC持续低于阈值） | 降权（watch→degraded→disabled） |
| 覆盖率断崖（覆盖率突降>20%） | 暂停该因子 |
| 风险超阈（组合风控维度超限） | 降仓 |
| 数据异常（PIT失败率>5%） | 中止信号生产 |

## 14.7 告警阈值

| 指标 | 阈值 | 说明 |
|------|------|------|
| IC均值 | < 0.02 | 因子可能失效 |
| IR | < 0.5 | 因子稳定性不足 |
| IC t值 | < 2 | 因子统计不显著 |
| PSI | > 0.25 | 因子分布显著漂移 |
| 模块相关 | > 0.85 | 模块间共线性过高 |
| 覆盖率 | < 50% | 因子数据严重不足 |

---

# 15. 性能优化设计

---

## 15.1 因子截面计算优化

### 15.1.1 向量化替代逐股票循环
原实现：`for ts_code, group in price_window.groupby('ts_code')` 逐股票循环计算因子。

优化：使用 `pandas.groupby().shift()` 和 `unstack` 向量化计算动量因子、前瞻收益等，仅波动率/流动性/微观结构因子保留逐股票计算（涉及 rolling 等复杂操作）。

### 15.1.2 因子预计算缓存
回测引擎的 `signal_generator` 在每个调仓日重新调用 `calc_cross_section_factors()`，造成大量重复计算。

优化：回测前预计算所有调仓日截面因子，存入进程内缓存 `_factor_cache`，`signal_generator` 直接读取。

### 15.1.3 IC计算并行化
原实现：串行遍历 `calc_dates` 计算各日期IC。

优化：`ProcessPoolExecutor` 并行计算各日期截面因子IC，充分利用多核CPU。

---

## 15.2 IC衰减计算优化

原实现：`calc_factor_decay` 对每个 lag (1-20) 调用 `_get_forward_returns()`，每次执行一次 DB 查询，共20次查询。

优化：一次查询 `trade_date` 到 `trade_date + max_lag + 60` 的所有行情数据，再向量化计算各 lag 的前瞻收益（`groupby().shift(-lag)`），将20次 DB 查询降为1次。

---

## 15.3 模型打分优化

### 15.3.1 批量查询消除N+1
原实现：`calculate_scores()` 对每个因子权重分别查询 `Factor` 和 `FactorValue`，13+因子产生26+次查询。

优化：`Factor.id.in_(factor_ids)` 批量查询因子定义，`FactorValue.factor_id.in_(factor_ids)` 批量查询因子值，2次查询替代26+次。

### 15.3.2 前瞻收益向量化
原实现：`_get_forward_return_data()` 逐股票循环计算前瞻收益。

优化：`groupby('security_id')['close'].shift(-forward_period)` 向量化计算。

---

## 15.4 数据同步优化

### 15.4.1 批量存在性检查
原实现：逐行 `SELECT ... WHERE ts_code=? AND trade_date=?` 检查存在性再 `INSERT`，5000股×500天=250万次查询。

优化：`IN` 批量查询已存在的 `(ts_code, trade_date)` 键集合，Python端过滤后 `bulk_save_objects` 批量写入。

### 15.4.2 线程并行
原实现：串行 `for` 循环同步股票数据。

优化：`ThreadPoolExecutor(max_workers=4)` 并行同步，IO密集型任务充分利用等待时间。

---

## 15.5 回测引擎优化

| 优化项 | 原实现 | 优化后 |
|--------|--------|--------|
| price_data构建 | `iterrows` 逐行 | `itertuples` (5-10x加速) |
| 因子重复计算 | 每调仓日重算 | 预计算缓存 |
| 因子截面计算 | 逐股票循环 | 向量化 groupby |

---

## 15.6 缓存体系

| 缓存实例 | 容量 | TTL | 驱逐策略 | 用途 |
|----------|------|-----|---------|------|
| `cache_service` | 2000 | 300s | LRU | 通用热点查询 |
| `factor_cache` | 5000 | 600s | LRU | 因子值、IC分析结果 |
| `_factor_cache` | 无限 | 进程生命周期 | 无 | 回测截面因子预计算 |

缓存服务特性：
- TTL过期自动清理
- LRU驱逐防止内存溢出
- 命中率统计 `stats()` 支持监控

---

## 15.7 预期性能提升

| 场景 | 优化前 | 优化后 | 提升倍数 |
|------|--------|--------|---------|
| 因子值查询(5000股) | 5000次SELECT | 1次批量+缓存 | ~50x |
| IC衰减计算(20lag) | 20次DB查询 | 1次查询+向量化 | ~20x |
| 数据同步(5000股) | 串行+逐行检查 | 4线程+批量检查 | ~10x |
| 回测信号生成 | 每次重算因子 | 预计算缓存 | ~20x |
| stock_daily查询 | 单列索引扫描 | 复合索引直接定位 | ~10-100x |

---

# 16. 机器学习增强算法设计

---

## 16.1 两阶段结构

V2.0采用两阶段结构，已实现基线版本：

- **第一阶段**：模块内聚合 — 每个模块内因子加权Z-score
- **第二阶段**：ML融合层 — 将模块分数作为特征输入ML模型

---

## 16.2 ML目标定义

ML模型目标是**横截面排序预测**，不是绝对收益预测：
\[
\min \sum_{i} L(rank\_pct(\hat{y}_i), rank\_pct(y_i))
\]

---

## 16.3 训练样本构造

- 按调仓日取样（非每个交易日）
- 只在core_universe_v2内取样
- 标签截尾winsorize到1%/99%
- 线性模型使用Z-score标准化标签，树模型使用原始截尾标签

---

## 16.4 特征分层设计

| 层级 | 特征 | 数量 |
|------|------|------|
| 第一层 | 模块分数（质量成长/预期修正/残差动量/资金流确认/风险惩罚） | 5 |
| 第二层 | 风格与状态（市值/波动率/流动性/Beta/行业哑变量/市场宽度/VIX等效） | 8~15 |
| 第三层 | 市场环境（Regime哑变量/择时信号/北向趋势） | 5~10 |

总特征数控制在20~50个。

---

## 16.5 模型选择

| 模型 | 类型 | 用途 |
|------|------|------|
| Ridge | 线性 | 基线线性融合 |
| ElasticNet | 线性 | 稀疏特征选择 |
| LightGBM Regressor | 树模型 | 非线性融合 |
| LightGBM Ranker | 树模型 | 排序优化 |

---

## 16.6 训练参数

| 模型 | 参数 | 值 |
|------|------|-----|
| Ridge | alpha | 3.0 |
| ElasticNet | alpha | 0.001 |
| ElasticNet | l1_ratio | 0.15 |
| LightGBM | learning_rate | 0.03 |
| LightGBM | max_depth | 4 |
| LightGBM | num_leaves | 31 |
| LightGBM | min_child_samples | 200 |
| LightGBM | subsample | 0.8 |
| LightGBM | colsample_bytree | 0.8 |
| LightGBM | reg_alpha | 0.1 |
| LightGBM | reg_lambda | 1.0 |

---

## 16.7 训练与验证

- 训练窗口：36个月
- 验证窗口：12个月
- 重训练频率：月度
- 验证方式：Walk-Forward OOS

---

## 16.8 融合方式

三路融合：
\[
score_{final} = 0.30 \times score_{rule} + 0.25 \times score_{ridge} + 0.45 \times score_{lgbm}
\]

---

## 16.9 预测置信度

双模型一致性计算：
\[
conf = 1 - |rank\_pct_{linear} - rank\_pct_{tree}|
\]

置信度低时，倾向使用规则基线。

---

## 16.10 回退机制

保留规则基线作为fallback：
\[
score_{fallback} = \sum_{m} w_m \times S_{module,m}
\]

当ML模型异常（预测分布漂移/OOS偏差超阈值/模型加载失败）时，自动切换到fallback_score。

---

## 16.11 防过拟合规则

| 规则 | 说明 |
|------|------|
| PIT严格 | 所有财务数据严格PIT对齐，禁止未来函数 |
| 特征数≤50 | 控制特征维度，防止高维过拟合 |
| 稳定性筛选 | 仅使用IC持续为正的因子/模块 |
| 共线性控制 | 高相关特征(>0.7)只保留一个 |
| 模型上线闸门 | OOS IC>0.03且IR>0.5才可上线 |

---

## 16.12 后续可扩展方向

### 16.12.1 文本增强因子
- 公告情绪
- 财报文本变化
- 新闻主题信号

### 16.12.2 高级融合
- stacking多层融合
- Attention权重动态融合

### 16.12.3 在线学习
- 增量训练
- 概念漂移自适应

---

# 17. 默认参数建议

| 参数 | 默认值建议 |
|---|---|
| 上市天数过滤 | 180天 |
| 流动性阈值 | 8000万 |
| 市值过滤 | 80亿 |
| 价格过滤 | 3元 |
| 流动性窗口 | 20日 |
| 动量窗口 | 20/60/120/250日 |
| 波动率窗口 | 20/60日 |
| 调仓频率 | 周频 / 双周 / 月频 |
| 目标持仓数 | 30~100，视基准而定 |
| 单股上限 | 3% |
| 行业偏离上限 | 5% |
| 换手上限 | 按策略类型设定 |
| 因子去极值 | MAD 或 1%/99% |
| 标准化方式 | 横截面 Z-score |
| 中性化方式 | 行业 / 市值回归残差 |
| 模块权重（质量成长） | 35% |
| 模块权重（预期修正） | 30% |
| 模块权重（残差动量） | 25% |
| 模块权重（资金流确认） | 10% |
| 风险惩罚系数λ | 0.35 |
| 动态权重收缩λ | 0.7 |
| 择时4档仓位 | 30% / 60% / 80% / 100% |
| 组合Top60分层赋权 | 1~10名1.5x / 11~30名1.2x / 31~60名1.0x |
| 单票上限 | 3% |
| 行业偏离 | 5% |
| ML Ridge alpha | 3.0 |
| ML LightGBM lr | 0.03 |
| ML LightGBM depth | 4 |
| ML LightGBM leaves | 31 |
| ML三路融合权重 | 规则0.30 / Ridge0.25 / LightGBM0.45 |

---

# 18. 验收标准

## 18.1 算法实现验收
- 所有因子计算结果可复现
- 所有中性化结果可验证
- 多因子打分逻辑可追踪
- 组合生成逻辑可解释
- 回测规则与 A 股制度一致

## 18.2 研究验收
- 至少形成一套稳定的多因子组合
- 至少一个基准下获得正向超额
- 因子有效性报告完整
- 风险暴露和换手率在可接受范围内

## 18.3 生产验收
- 调仓信号可按期输出
- 风险监控可运行
- 模型结果可审计
- 模型异常可告警

---

# 19. 结论

V2.0升级核心：做减法（主策略收敛最稳版本）、做深度（每个因子/模块/风险源更严谨）、做差异化（事件风控/预期差/风险排雷）。

本算法设计说明书定义了 A 股多因子增强策略平台的核心算法框架。
其设计原则不是追求复杂度，而是优先保证：

- 数据点时一致
- 因子逻辑清晰
- 评分方法稳健
- 组合可交易
- 回测真实可信
- 风控可执行
- 输出可商业化交付

V2.0已实现的关键升级：
- ML增强排序（Ridge + LightGBM + 规则fallback三路融合）
- 择时简化为风险预算（4档仓位+回撤保护）
- 组合构建采用分层赋权+风险折扣
- 新增风险模型与风控算法（三层风控体系）
- 新增事件中心与风险惩罚模块
- 因子体系升级为5模块+1风险模块架构
- 全链路5类监控与自动响应机制

---

# 20. 版本历史

| 版本 | 日期 | 变更说明 |
|---|---|---|
| V1.0 | 2026-04-01 | 初始版本 |
| V1.1 | 2026-04-13 | 新增因子状态机、因子正交化选择、过拟合检测、性能优化 |
| V1.2 | 2026-04-22 | 新增分红派息处理、PIT管理算法、幸存者偏差修正算法、数据质量检查算法；修正因子预处理顺序；T+1精确规则；涨跌停细化板块差异；面板数据安全 |
| V2.0 | 2026-04-24 | V2重大升级：5层架构→6层架构（新增风险与执行层、监控与治理层）；因子体系6类→5模块+1风险模块；新增因子元数据系统与7关研究流程；数据层新增事件数据/快照版本管理/PIT严格管理；预处理8步→10步；标签体系更新（主标签20日超额/分类标签/截尾标准化）；股票池参数收紧（180天/8000万/80亿/3元）+风险事件过滤+黑名单；评分模型重构为模块打分+ML融合+三路融合+动态权重收缩+Regime调权+模块状态机+预测置信度+模型注册表；择时简化为风险预算4档仓位+回撤保护；组合构建新增分层赋权+风险折扣+双轨制+100股整数倍；新增风险模型与风控算法（协方差估计/Barra/VaR/黑名单/规则引擎/三层风控/极端情景保护）；回测新增稳健性/压力/归因/容量/Walk-Forward/统计检验；监控扩展为全链路5类监控+自动响应+告警阈值；ML增强从扩展方向升级为已实现V2基线（两阶段结构/三路融合/防过拟合规则/回退机制）；默认参数全面更新 |
| V2.1 | 2026-04-24 | 基于业务逻辑详解迭代完善：数据类别补充业务用途说明（8类数据各自在因子/择时/风控中的角色）；新增数据质量检查章节（3.3节，设计原则"宁可保守也不让错误数据污染信号"）；股票池补充业务原理（第一道风控、核心池vs扩展池含义）；过滤规则补充业务原因（每条过滤规则的"为什么"）；因子体系补充模块预测目标周期表（价格5-10日/价值10-20日/修正5-20日/资金流3-10日）；标签补充"为什么不预测绝对收益"的业务原理；信号融合补充5步完整流程（基础权重→动态IC→Regime调权→高相关收缩→归一化融合）；中性化原理补充"为什么必须在标准化之前" |